VITE_API_BASE_URL=http://localhost:4000

# Sentry error tracking (src/main.jsx). Get this from your own Sentry
# project: sentry.io -> Create Project -> React -> Settings -> Client Keys
# (DSN). Optional — if unset, Sentry is skipped entirely.
VITE_SENTRY_DSN=
